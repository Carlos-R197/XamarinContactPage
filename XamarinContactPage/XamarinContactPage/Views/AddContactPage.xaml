<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinContactPage.Views.AddContactPage"
             Title="New Contact">

    <StackLayout>
        <Entry Placeholder="Name" Text="{Binding Name}" Style="{StaticResource entryStyle}"/>
        <Entry Placeholder="Number" Text="{Binding Number}" Style="{StaticResource entryStyle}" Keyboard="Numeric"/>

        <Button  Text="Add"  Command="{Binding AddContactCommand}"
                Style="{StaticResource buttonStyle}" />
    </StackLayout>
</ContentPage>